# TODO — Idées pour v2
- [X] Prochains trains Ligne P intégrés aux créneaux gym
- [X] Compte à rebours "dans X min"
- [X] Carte gare Trilport (départs + arrivées)
- [X] Bascule de thème Bureau / Cinéma
- [X] Bandeau trafic avec alerte visuelle
- [X] Infos trafic Ligne P (perturbations)
- [X] Indicateur visuel du "meilleur créneau" (basé sur le score)
- [X] Refactoring JS — séparer en modules (api.js, utils.js, index.js)
- [X] Refactoring CSS — réorganiser et nettoyer les règles redondantes
- [X] Redesign 5 cartes single-page (voir docs/ui-design.md)
- [X] Panneaux "Voir départs/arrivées/créneaux" + pluie se referment seuls après 10s
- [X] Icône météo colorée selon la température (bleu → vert → orange → rouge)
- [X] Vraie image de train (Ligne P) dans la carte trafic, qui glisse en continu et s'arrête/casse visuellement en cas d'alerte
- [X] Animation météo de fond (soleil/nuage/pluie/orage/neige/brouillard) derrière la carte météo
- [X] Icônes SVG pour les verdicts gym (au lieu des emoji ✅🌧️🥶☀️)
- [X] Créneau gym "vitrine" = le plus proche à venir, pas systématiquement le premier de la liste
- [X] Badge météo Meaux réaffiché dans l'en-tête de la carte gym
- [X] Panneaux "tucked away" : un tap dedans relance leur propre minuterie de 10s (bug LL-2)
- [X] Cartes départ/arrivée : même hauteur garantie, quel que soit le nombre de trains à venir
- [X] Cartes départ/arrivée : même largeur garantie (grid minmax(0, …), pas juste "Nfr")
- [X] iPad (portrait comme paysage) garde météo/départ/arrivée sur une seule rangée
- [X] Refresh 60s invisible — fusion DOM ciblée au lieu d'un replaceChild complet (plus de fondu/reset d'animation à chaque cycle)
- [X] Barre démo : boutons pour forcer chaque condition météo et chaque niveau trafic (dont "en panne"), combinables, activables un par un
- [X] Barre démo : panneau "Scénarios" avec les combinaisons météo × trafic en un clic
- [X] Animation brouillard améliorée (nappes floutées superposées au lieu de barres plates)
- [X] Train en panne : fond mal assorti corrigé (rectangle de fond du SVG tracé supprimé)
- [X] Carte trafic : "Détails"/"Travaux" remplacés par des icônes coin (i / cône travaux) + popup
- [X] Nouveau niveau trafic "ailleurs" (perturbation hors trajet) : train qui roule mais tressaute légèrement
- [X] Fix : la démo météo corrompait le cache météo partagé (15 min) au lieu de n'affecter que sa propre requête
- [X] Carte trafic : bouton "i" en icône seule (comme l'alerte des cartes train), pas de badge rond en plus
- [X] Alerte départ/arrivée plus visible (icône plus grande + anneau qui pulse)
- [X] Secousse "ailleurs" renforcée (plus forte, plus fréquente)
- [X] Bug LL-3 : popups trafic ne couvraient que 700px de haut, les clics traversaient en dessous
- [X] Bug LL-4 : `inset` non supporté par Safari iOS 12 — train/fond météo/popups pouvaient ne pas s'afficher (confirmé sur iPad Air iOS 12.5.8)
- [X] Bug LL-1 : `gap` sur flexbox non supporté par Safari iOS 12 — 13 règles corrigées (marges à la place)
- [X] Bug LL-5 : surlignage `:hover` restait collé après un tap sur l'iPad — 9 règles passées dans `@media (hover: hover)`
- [X] Phase de service (actif/bientôt/fin de service) : libellés plus clairs + train garé et endormi (Zzz) la nuit, boutons démo pour prévisualiser
- [X] Échelle de couleur trafic à 3 niveaux, pas 4 : "fluide" vert, "info"/"ailleurs" orange (même point + même lueur ambre discrète), "alerte" (touche Trilport) rouge et clignote en version "restrained" (creux du point à 0.5 au lieu de 0.25, lueur à 0.35 au lieu de 0.55) pour rester lisible sans devenir aussi criant qu'un retard de train important (LOOK-22, 4e révision — le bleu essayé pour "ailleurs" se lisait comme un signal positif/neutre plutôt que comme un niveau de sévérité intermédiaire). Toutes les lueurs de carte (train ET trafic) utilisent la même direction d'ombre (inset), pour la cohérence visuelle entre les deux types de carte.
- [X] Train "info"/"ailleurs" : traversée nettement ralentie par rapport au trafic fluide (14s vs 48s — le premier passage avait "ailleurs" quasi impossible à distinguer de "fluide" à l'œil, corrigé en creusant l'écart ; "info" ralenti lui aussi, mais sans le tressautement réservé à "ailleurs")
- [X] Lueur/clignotement de retard déplacé de l'heure elle-même vers la CARTE départ/arrivée entière (5–9 min = ambre doux et discret, 10 min et plus = corail chaud) — la carte trafic en alerte partage désormais le même système visuel
- [X] Toutes les animations "clignotantes" (point + lueur carte trafic, icône+anneau départ/arrivée, lueur de carte) resynchronisées par durée d'animation (pas par nom de keyframe) — des formes différentes partageant le même rythme restent maintenant perçues comme "elles clignotent ensemble", pas seulement synchronisées en théorie
- [X] Barre démo : deux témoins retard, léger à 5 min et rouge à partir de 10 min, pour prévisualiser les seuils exacts
- [X] Barre démo : bouton témoin "pluie" (15 min/1 h/16h58/2 jours/en cours/aucune) pour tester le texte "Pluie : dans X" sur tout délai, dont l'exemple exact du bug LL-8
- [X] Bouton "Pluie" ne réagissait plus au tap pour "en cours" (désactivé, rien à déplier) — reste cliquable désormais et révèle une petite pluie animée à la place d'une heure exacte
- [X] "Pluie : pluie en cours" (uniquement ce cas) : le tap REMPLACE le libellé par la petite pluie animée (même emplacement, pas un panneau qui s'ouvre en dessous) et revient tout seul après 10s — le cas "Pluie : dans X" garde son ancien comportement (le libellé reste, l'heure exacte s'ouvre en dessous)
- [X] Bug LL-8 : texte "Pluie : dans X h" incohérent avec l'heure exacte affichée (arrondi à l'heure supérieure)
- [X] Bug LL-9 : une perturbation sur une autre branche de la ligne P (ex. Provins) déclenchait à tort "alerte" (touche Trilport) — `ARRETS_TRAJET` matchait sur le terminus partagé Paris-Est/Gare de l'Est
- [X] Bug LL-7 (boutons/menus) — **confirmé corrigé sur le vrai iPad** : séparateurs numériques (`10_000`) dans `disclosure.js`/`weather-details.js` faisaient planter le parsing de ces fichiers sur Safari 12 (aucun clic ne s'enregistrait). Corrigé (`10000`), plus `.flatMap()` remplacé dans `refresh.js` par prudence.
- [X] Système de lueur de carte réécrit en `::after` + opacité (pas de `box-shadow` animé directement) et `filter: drop-shadow()` du train retiré — coût de repaint sur du matériel ancien
- [ ] Animation encore saccadée sur l'iPad réel malgré le correctif LL-7 (boutons OK désormais, mais pas la fluidité) — repéré : la carte trafic saccade tout l'écran dès qu'elle est visible, fluide dès qu'elle sort de l'écran. Cause probable trouvée : `train-profil.svg` (état normal, glissement quasi permanent) avait ~1300 `<path>` individuels (tracé photo via imagetracerjs) — remplacé par un PNG raster fourni par l'utilisateur (`train-ligne-p.png`), moins coûteux à faire glisser en continu sur du matériel faible. **Pas encore reconfirmé sur l'appareil réel.** `train-en-panne.svg` (encore plus dense, ~1900 `<path>`) n'a pas reçu le même traitement, candidat si la saccade persiste spécifiquement lors d'une alerte.
- [X] Animation soleil chaud plus expressive (LOOK-29) : disque central visible + rayons qui tournent, pulse transform/opacity (pas de box-shadow/filter animé), variante "chaude" dès 32°C (pulse plus ample, plus rapide). Barre démo : "Soleil" (24°) et "Soleil très chaud" (38°) désormais distincts visuellement. Chromium-vérifié (classes CSS, opacité/durée d'animation, captures), pas encore confirmé sur l'iPad réel.
- [X] Barre démo simplifiée (LOOK-30) : bouton/tiroir "Scénarios" (combinaisons météo×trafic) retiré, ainsi que le code serveur associé (`construireScenariosDemo`) — chaque axe (météo/trafic/service/retard/pluie) reste indépendamment combinable et les axes se préservent mutuellement, sans matrice de combinaisons séparée à maintenir.
- [X] Bug trouvé en vérifiant LOOK-29 : les boutons météo démo forçaient le code/température mais pas `is_day` — testés le soir/la nuit (comme maintenant), "Soleil"/"Soleil très chaud" ne montraient donc rien (`sceneMeteo` retombait sur "calme"). Corrigé : le mode démo météo force aussi `is_day = 1` (toutes les scènes démo sont pensées de jour).
- [X] Icône soleil (carte météo, badge Meaux, boutons démo météo) : après plusieurs itérations (cercle seul → 4 rayons diagonaux → 8 rayons courts avec espace visible → dégradé continu 12-30°C), simplifié en deux états nets à un seul seuil (`traceSoleil()`/`couleurSoleilIcone()`, `services/icons.js`) — à 30°C et en dessous : "normal", orange (#f5a524), rayons à MOITIÉ de la longueur canicule ; au-dessus de 30°C : "canicule", rouge (#f2555a), pleine longueur. `SOLEIL_ICONE_RAYON_NORMAL` dérive de `SOLEIL_ICONE_RAYON_CANICULE / 2`, donc les deux ne peuvent pas diverger si l'un des deux est retouché. Couleur posée directement sur le `<svg>` (prioritaire sur la couleur générale de `couleurTemperature()` posée par le conteneur parent). L'icône "lunettes" du verdict gym réutilise la même clé mais sans température (reste à la longueur canicule par défaut), ce n'est pas une question de chaleur là-bas.
- [X] Bulles de chaleur retirées de l'animation soleil (LOOK-29, variante très chaude) — juste le disque + la lueur qui grossissent/pulsent plus fort au-delà de 32°C, sans bulles montantes
- [X] Carte gym (LOOK-32) : tentative de reconstruction en profondeur (créneaux live Trilport↔Meaux, fenêtres aller-retour calculées, météo par trajet, démo dédiée) **entièrement annulée à la demande de l'utilisateur** — le résultat visuel (grille de 5 colonnes × plusieurs lignes) ressemblait à un tableau de gare, pas à une carte de recommandation, et ce n'était pas ce qui avait été demandé ("je n'ai jamais dit que ce que j'avais était mauvais, je voulais juste voir si tu pouvais l'améliorer"). Tout le code lié (`services/utils.js`, `services/api.js`, `services/icons.js`, `index.js`, `views/index.ejs`, `public/styles/main.css`, `public/scripts/gym-details.js`) a été restauré à l'identique du dernier commit (`983b3ab`) — la carte gym est revenue à sa version d'origine (LOOK-3 : résumé Départ Trilport/Météo/Retour conseillé + "Voir tous les créneaux"). Seule différence gardée : le libellé "Gym" au lieu de "Gym ce soir" dans l'en-tête, à la demande explicite de l'utilisateur. Idée de fenêtres aller-retour live pas abandonnée en soi, mais toute reprise future devra éviter le rendu "tableau" et repartir d'un vrai concept visuel validé par l'utilisateur avant d'implémenter.
- [X] Carte gym (LOOK-32 v3, reprise réussie, état final) : `construireTrajetsGym` (`services/utils.js`) classe les trajets Trilport↔Meaux par météo (meilleure en premier, pas le plus proche), avec une durée réelle bornée 1h-2h (`dureeMinGymMinutes`=60, `dureeMaxGymMinutes`=120) et un repli "horaire attendu" (aller :30/:38 chaque heure ; retour = grille RÉELLE et IRRÉGULIÈRE mesurée par l'utilisateur — semaine : 17h15/45, 18h15/30/45, 19h15/45, 20h15/45, 21h15, PUIS UN TROU jusqu'à 22h15/45 ; week-end : un train par heure à :15 — `HORAIRES_RETOUR_SEMAINE`, `creerTrainsRetourAttendus()`, jamais extrapolée au-delà de la liste, un "21h45" n'existe pas) quand les données IDFM live ne couvrent pas encore le créneau (ex. vérifié à 7h/11h pour un aller du soir) ; tout horaire estimé porte un badge "ESTIMÉ", jamais confondu avec un train live. Les créneaux déjà passés disparaissent en temps réel (borne mini = `max(heureMin, maintenant)`). Distinction départ/train : la carte affiche l'heure à laquelle QUITTER (maison ou salle), pas l'heure du train — le départ maison est `train - 8 min` arrondi à la dizaine inférieure (ex. train 17h30 → partir 17h20), le départ salle est `train retour - 15 min` (scooter+trajet+quai) déjà rond par construction ; le train lui-même reste affiché en petit texte secondaire ("train 17:30"). Pour une même heure, TOUTES les combinaisons (aller × retour) valides dans la fourchette 1h-2h sont gardées comme "sous-créneaux" à faire défiler (produit cartésien, pas un seul retour par aller) : ex. allers :30/:38 + retours possibles → `17h20-19h00`, `17h20-19h30`, `17h30-19h00`, `17h30-19h30`. Visuel : une seule fenêtre "vitrine" (3 colonnes Départ/Météo/Retour, alignées en `justify-content: space-between` pour matcher la liste compacte en dessous) affichée à la fois. Deux animations distinctes : entre sous-créneaux d'une même heure, roulement vertical façon horloge digitale (`.gym-sous-stack`/`.gym-sous-active`, 7s par variante — assez pour lire, tient jusqu'à ce que toutes les variantes de l'heure soient montrées) ; entre heures différentes, glissement latéral façon carrousel (`data-etat` suivante/actif/précédente). Un tap sur les fenêtres met la rotation en pause (reprend au retap, à un clic ailleurs sur la page, ou seule après 10s d'inactivité). La ligne compacte correspondante se replie/déplie en même temps dans la liste toujours visible en dessous. Bouton "liste" dans l'en-tête ouvre un popup listant tous les trajets classés (réutilise `disclosure`/`popup-fond`). Le libellé "Gym" est lui-même le bouton "séance terminée" : un tap secoue la carte puis replie tout sauf l'en-tête (état mémoire serveur `gymTermineDate`, `POST /gym/terminer`, se réinitialise seul le lendemain, retap pour annuler) ; ignoré en mode démo pour ne jamais masquer un scénario testé.
- [X] Bouton gym caché les dimanches (avec override possible) : par défaut le dimanche, la carte affiche "Jour de repos" avec une animation "Zzz" flottante (même motif que le train endormi hors service) au lieu des fenêtres. Révéler les trajets se mérite : il faut 3 taps sur "Gym" — les deux premiers répondent par une blague aléatoire (FR/EN mélangés, `BLAGUES_DIMANCHE` dans `public/scripts/gym.js`, jamais la même que les 2-3 précédentes) sans rien débloquer, le 3e bascule pour de vrai (même état mémoire que "séance faite", interprété à l'envers ce jour-là — `estDimancheParis()`, `services/utils.js`). Démo dédiée `?demo=1&gym=dimanche` (`NIVEAUX_GYM_DEMO`) pour tester tout le flux sans attendre un vrai dimanche ; le clic fonctionne à l'identique en démo (`POST /gym/terminer` reçoit le contexte de page via `location.search`). Les démos de données (plein/un/indisponible) n'affectent jamais l'état réel — un clic pendant ces démos ne touche plus `gymTermineDate` (bug trouvé et corrigé : ça polluait avant l'état partagé de l'iPad).
- [X] LOOK-35 — toucher le résumé visible de la carte trafic ouvre le même détail que l'icône info, y compris pour les messages `INFORMATION` orange.
- [X] LOOK-43 — prochain retour Meaux → Trilport affiché en permanence à côté du badge météo Meaux, dans l'en-tête de la carte gym (donc visible même dimanche/séance terminée, quand `.gym-corps` est masqué). Source : `departsRetour` (déjà utilisé pour les trajets gym), pas une donnée démo dédiée — même règles de ponctualité/retard (`retardNiveau`, `ponctualiteLibelle`) que les cartes Départ/Arrivée principales, mais bleu (`var(--info)`) au repos plutôt que vert/rouge ; ambre à 5 min de retard, rouge à 10 min, comme ailleurs. Train déjà passé → le suivant apparaît automatiquement (filtré par `filtrerDeparts`, comme le reste). Fin de service réel → libellé "Fin de service" (même `statutService()` que Départ/Arrivée), avec le même gating `service` de démo. Barre démo dédiée (`?demo=1&retourMeaux=aLheure|moyen|fort|fin`, `NIVEAUX_RETOUR_MEAUX_DEMO`) pour prévisualiser les 4 états sans attendre un vrai train en retard.
- [X] Sélection verrouillée d'un créneau gym : taper une ligne du popup "Tous les trajets" (`#gym-popup .gym-popup-liste`, chaque `<li>` désormais `role="button"`/`tabindex` + un `<template>` interne portant le rendu exact de ce sous-créneau précis, puisque le popup liste des sous-créneaux individuels de tous les trajets, pas seulement les 3 groupes affichés en vitrine) l'affiche en vitrine (`.gym-colonnes`), referme le popup, et y reste épinglée — rotation automatique suspendue — jusqu'à sa propre expiration (`t.departSalleHeure`, le "quitter la salle" de ce sous-créneau précis, ex. 18h30→20h30 expire à 20h30 ; `services/utils.js` calcule aussi `expirationHeure` au niveau du groupe mais la sélection utilise le sous-créneau exact choisi). La liste compacte sous la vitrine (`.gym-alternative[data-index]`) reste, elle, non cliquable — seul le popup sélectionne. Le client (`public/scripts/gym.js`, `verrouille`/`appliquerVerrouillage`) construit une fenêtre à partir du `<template>` cloné et la réinjecte si la fusion DOM du rafraîchissement 60s l'a retirée (le train aller peut avoir disparu des données serveur pendant que le créneau reste jouable) ; à l'expiration, la rotation normale reprend au choix du serveur plutôt que de repartir du créneau expiré. Fonctionne avec les données réelles et tous les scénarios démo gym existants (même mécanisme serveur). Seule la vitrine est figée : la liste compacte en dessous continue sa propre rotation (masquage/affichage des alternatives) normalement pendant qu'un créneau reste verrouillé — le verrouillage se réapplique par-dessus l'état "normal" à chaque tick plutôt que de couper toute la rotation. Lignes du popup survolables (`@media (hover: hover)`, fond `var(--surface-2)`) pour signaler qu'elles sont sélectionnables, comme les autres boutons de la barre démo.
- [ ] Migration vers React (composants, countdown temps réel)
- [ ] Lunettes : tenir compte de l'heure aussi en hiver
- [ ] Météo de demain (toggle ou onglets)
